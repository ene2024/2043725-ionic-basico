import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'miApp-2043725',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
